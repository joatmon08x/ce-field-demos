# Ledgerly does not need environment variables for the happy path.
# Prisma SQLite is file:./dev.db relative to prisma/schema.prisma.
# Copy this file only if you add a secret later — never commit one.
